<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.yedam.silhyun.common.map.CommnetMapper">
	
	<!-- 댓글 리스트 -->
	<select id="commentList">
		select id, cntn, com_date
		from commment
		where ctgr_num = 12
		and dep = 0
		order by grp, dep, com_num
	</select>

	<!-- 댓글 작성 -->
	<insert id="insertComment">
		insert into 
			commment
					(com_num
					,ctgr
					,ctgr_num
					,dep
					,grp
					,id
					,cntn
					,com_date
					)
			values
				    ( seq_comno.nextval
				    , C
				    , 12
				    , 0
				    , seq_grpno.nextval
				    , hi123
				    , sysdate
				    )
					
	</insert>
</mapper>